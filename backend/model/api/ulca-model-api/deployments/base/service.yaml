apiVersion: v1
kind: Service
metadata:
  name: model-api-service
  namespace: model-api
  labels:
    app: model-api
spec:
  selector:
    app: model-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
