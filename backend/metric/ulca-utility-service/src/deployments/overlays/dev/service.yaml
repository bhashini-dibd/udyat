apiVersion: v1
kind: Service
metadata:
  name: utility-service-service
  namespace: utility-service
  labels:
    app: utility-service
spec:
  type: ClusterIP
  selector:
    app: utility-service
  ports:
    - name: http
      port: 80
      targetPort: 5001
