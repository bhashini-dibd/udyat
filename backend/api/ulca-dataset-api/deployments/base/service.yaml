apiVersion: v1
kind: Service
metadata:
  name: dataset-api-service
  namespace: dataset-api
  labels:
    app: dataset-api
spec:
  selector:
    app: dataset-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
