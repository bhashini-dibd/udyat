apiVersion: v1
kind: Service
metadata:
  name: master-data-management-service
  namespace: master-data-management
  labels:
    app: master-data-management
spec:
  type: ClusterIP
  selector:
    app: master-data-management
  ports:
    - name: http
      port: 80
      targetPort: 5001
