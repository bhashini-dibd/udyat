apiVersion: v1
kind: Service
metadata:
  name: zuul-gw-service
  namespace: zuul-gw
  labels:
    app: zuul-gw
spec:
  selector:
    app: zuul-gw
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
