apiVersion: v1
kind: Service
metadata:
  name: asr-model-compute-service
  namespace: asr-model-compute
  labels:
    app: asr-model-compute
spec:
  selector:
    app: asr-model-compute
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: ClusterIP
